import 'package:flutter/material.dart';

class App4Page extends StatelessWidget {
  const App4Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('App 4')),
      body: const Center(child: Text('Welcome to App 4!')),
    );
  }
}
