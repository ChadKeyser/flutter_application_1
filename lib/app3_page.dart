import 'package:flutter/material.dart';

class App3Page extends StatelessWidget {
  const App3Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('App 3')),
      body: const Center(child: Text('Welcome to App 3!')),
    );
  }
}
